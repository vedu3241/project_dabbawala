import 'package:flutter/material.dart';

class HireScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Hire Page", style: TextStyle(fontSize: 24))),
    );
  }
}