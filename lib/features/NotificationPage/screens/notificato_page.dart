import 'package:flutter/material.dart';

class NotificatonPage extends StatelessWidget {
  const NotificatonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Notificaton Screen'),
      ),
    );
  }
}