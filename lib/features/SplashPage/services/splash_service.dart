import 'dart:async';


import 'package:flutter/material.dart';
import 'package:get/get.dart';


class SplashServices {
  // void isLogin(BuildContext context) {
  //   final auth = FirebaseAuth.instance;
  //   final user = auth.currentUser;

  //   // Set a timer for the splash screen duration (3 seconds)
  //   Timer(Duration(seconds: 3), () {
  //     // Check if the user is logged in or not and navigate accordingly
  //     if (user != null) {
  //       // User is logged in, navigate to the HomePage
  //       Get.offAll(() => Homepage());  // Use Get.offAll() to clear the stack and prevent going back to the splash screen
  //     } else {
  //       // User is not logged in, navigate to the LoginScreen
  //       Get.offAll(() => LoginScreen());
  //     }
  //   });
  // }
}
